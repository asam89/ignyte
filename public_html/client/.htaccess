# Session cookie security is set in admin/config.php via ini_set().
# php_flag directives were removed: they cause a 500 error when PHP runs
# as FastCGI/LSAPI (as on Hostinger) rather than as an Apache module.
